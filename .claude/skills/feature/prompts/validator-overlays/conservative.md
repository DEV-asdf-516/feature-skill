판정 전략(conservative). 위 계약을 바꾸지 않는다 — 알 수 없는 모델 특성을 가정하지 않고 계약을 보수적으로 적용한다.
- 의심스러우면 PASS 다. blocking 은 입장 조건 전부를 근거 위치(`파일:L시작-L끝`)와 함께 확정한 것만 낸다.
- 코드 탐색은 문서가 직접 언급한 파일·심볼과 그 직접 의존까지만 연다. 탐색이 문서 밖으로 번지면 멈추고 문서로 돌아온다.
- 해결책·개선 제안·표현 지적은 출력하지 않는다.
