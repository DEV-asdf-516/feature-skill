# REQUIRED / DELEGATED 판정 사례 — solution shape 경계

`approach.md` 의 결정 단위를 REQUIRED 로 올릴지 DELEGATED 로 둘지 판정하는 기준은 **기술 범주가 아니라 결정의 영향**이다. 이 문서는 오케스트레이터(문서 작성자)·검증자·워커가 같은 경계를 쓰는지 사람이 대조하는 사례집이다. 실제 모델 호출 회귀(`validator-regression.sh`·`reviewer-regression.sh`)와 달리 유료 호출이 없다 — approach.md 를 쓰거나 검토할 때 아래 표에 대보는 용도다.

## 규칙

```
               ┌─ 외부 관찰 동작 / 영속 데이터 정합성 / 보안·권한·개인정보 경계 / 사용자 명시 방식·재사용 → 항상 REQUIRED
모든 결정 ─────┤
               └─ 그 외 → 워커가 고르면 solution shape 가 달라지는가?
                          ├─ 예  → REQUIRED (기법 + 근거를 적는다)
                          └─ 아니오 → DELEGATED (기법을 적지 않는다)
```

solution shape = 해결 전략과 주요 데이터/제어 흐름, 비용 특성(시간·공간·I/O·동기화), 기존 기능 재사용 vs 새 구현, 새 구조물(헬퍼·클래스·모듈·의존성) 필요 여부, 실패 방식.

판별법 한 줄: **유능한 두 개발자가 같은 요구를 받고 서로 다른 "접근법"을 고를 수 있으면 REQUIRED, 같은 접근법을 코드로 다르게 표현할 뿐이면 DELEGATED.**

과잉 설계 안전장치 두 개:
1. 애매하다고 REQUIRED 로 올리지 않는다. 갈리는 접근법과 그 영향(흐름·비용·재사용·구조물·실패 방식 중 하나)을 한 줄로 적을 수 없으면 DELEGATED 다.
2. 이번 변경의 직접 범위(같은 모듈·직접 의존 코드)에 같은 종류의 문제를 푸는 **명백한 단일 precedent** 가 있고 경쟁 패턴을 골라야 할 근거가 없으면 그 precedent 가 접근법이다. 워커 제약 ①("기존 패턴이 있으면 따른다")이 이를 고정하므로 approach.md 에 적지 않아도 된다. 저장소 전체에서 유일함을 증명하려 하지 않는다 — "하나를 발견했다"와 "경쟁 패턴이 없다"는 다른 명제이고, 검증자는 전체 감사를 하지 않으므로 세 역할이 같은 범위(직접 범위)를 봐야 한다. precedent 가 없거나 직접 범위 안에서 둘 이상 경쟁할 때만 REQUIRED 로 적는다.

REQUIRED 로 올린 뒤의 선택 품질 규칙 둘:
3. 후보 중 **가장 단순한 통상 해법**을 고른다. 직접 범위의 precedent → 언어·플랫폼·표준 라이브러리의 표준 기능 → 직접적인 최소 구현 순. 새 추상화·새 의존성·별도 상태 구조(상태 머신, 임시 버퍼, 사전 인덱스)는 앞선 방법보다 요구·기존 계약·필요한 비용 특성을 더 직접적이고 단순하게 만족한다는 구체적 이유가 있을 때만 고르고 이유를 적는다. "정확히 표현할 수 없을 때만" 으로 쓰면 선형 탐색도 요구를 만족하므로 사전 인덱스를 영영 못 고르는 O(n²) 숭배가 된다 — 비용 특성이 solution shape 의 일부라는 정의와 충돌한다. 소유권만 회수하고 이 규칙이 없으면 "수동 상태 머신으로 구현 [REQUIRED]" 가 나오고 워커는 이전보다 더 충실하게 장황한 코드를 만든다.
4. **금지는 기본적으로 쓰지 않는다.** 특정 대안이 요구 불변식이나 선택한 접근법을 깨뜨릴 때만 적고, 금지 이유와 지켜야 할 불변식(또는 허용 대안)을 함께 적는다. 수단 이름만 단독으로 금지하지 않는다 — "정규식 한 줄 치환 금지" 는 잘못된 형태이고, "토큰별 반복 치환은 순회를 n 번 하므로 쓰지 않는다. 한 번의 왼쪽→오른쪽 스캔을 유지한다" 가 올바른 형태다.

## 사례 10개

앞 5개는 REQUIRED 로 올려야 하는데 옛 기준("외부 동작이 안 바뀌면 DELEGATED")으로는 워커에게 넘어가던 결정이다. 뒤 5개는 새 기준을 남용하면 REQUIRED 로 올라가는데 실제로는 DELEGATED 여야 하는 결정이다. 둘 중 한쪽만 맞히는 판정은 실패다.

| # | 결정 | 기대 | 갈리는 접근법과 영향 (REQUIRED 라면 한 줄로 적혀야 함) | 함정 |
|---|---|---|---|---|
| 1 | 연속 중복 토큰을 한 번에 축약해야 한다. 정규식 `{2,}` 그룹 한 번 vs 문자 단위 수동 스캔 + 상태 변수 | REQUIRED | 해결 전략이 다르고, 수동 스캔은 상태 머신·임시 버퍼가 필요해 코드량이 몇 배가 된다(구조물·복잡도) | 결과 문자열이 같으니 "외부 동작 불변 → DELEGATED" 로 빠지는 것이 이번 사고의 형태다 |
| 2 | 목록 A 의 각 항목이 목록 B 에 있는지 확인. 항목마다 B 를 선형 탐색 vs B 를 먼저 Set 으로 만들어 조회 | REQUIRED | 비용 특성이 O(n·m) 과 O(n+m) 으로 다르고, 사전 인덱스는 중간 자료구조를 하나 더 만든다 | "자료구조 선택이니까"가 아니라 순회 횟수·비용이 갈리기 때문에 REQUIRED 다 |
| 3 | 화면에서 어떤 요소를 조건부로 강조. CSS 선택자·pseudo-class 로 해결 vs 상태 값을 추가하고 JS 에서 클래스를 토글 | REQUIRED | 새 상태(구조물)와 렌더 흐름이 달라지고, 한쪽은 코드가 거의 없다 | 백엔드 예시가 아니어도 같은 규칙이 먹는다. 기술 범주를 열거하지 않는 이유 |
| 4 | 항목 n 개의 상세를 가져온다. 한 번에 일괄 조회 vs 루프 안에서 n 번 개별 조회 | REQUIRED | I/O 횟수가 1 vs n 으로 갈린다(비용 특성) | "둘 다 같은 결과를 돌려준다"는 DELEGATED 근거가 아니다 |
| 5 | 입력 문자열 정규화가 필요한데 같은 모듈에 `normalizePhone` 과 `PhoneFormatter.canonical` 두 유틸이 공존한다 | REQUIRED | 재사용 vs 새 구현이 갈리고, 어느 유틸을 쓰느냐로 기존 호출자와의 일관성이 달라진다. 하나를 골라 줄 범위를 인용한다 | 직접 범위에서 precedent 가 **둘 이상 경쟁**하므로 안전장치 2 가 적용되지 않는다. 명백한 단일 precedent 였다면 적지 않아도 됐다 |
| 6 | 결과를 담을 컨테이너를 `ArrayList` 로 할지 언어 기본 mutable list 로 할지. 접근법은 이미 "한 번 순회하며 append" 로 정해져 있다 | DELEGATED | 순회 횟수·비용·구조물이 그대로다. 같은 접근법의 표현 차이 | "자료구조는 REQUIRED" 라고 단어에 반응하면 과잉 설계다. 판정 단위는 자료구조라는 명사가 아니라 solution shape 다 |
| 7 | 이미 REQUIRED 로 "정규식 한 번으로 찾는다"가 정해진 뒤, `Pattern` 을 static 상수로 둘지 메서드 지역 변수로 둘지 | DELEGATED | 접근법이 같고 흐름·비용·구조물이 바뀌지 않는다 | 사례 1 을 REQUIRED 로 올렸다고 그 아래 표현까지 내려주면 문서가 소설이 된다 |
| 8 | 조건 분기를 중첩 if 로 쓸지 early return 으로 쓸지, 순회를 for 로 쓸지 while 로 쓸지 | DELEGATED | 같은 한 번 순회·같은 결정점의 표현 차이 | 리뷰어의 "issue 가 아닌 것"과 같은 항목이다. 세 역할이 같은 경계를 써야 한다 |
| 9 | 예외를 응답 코드로 바꾸는 위치. 변경 대상 모듈이 타는 전역 예외 핸들러가 **명백한 단일 precedent** 로 있고 이번 피처도 그 흐름을 탄다 | DELEGATED | 직접 범위의 precedent 가 접근법을 이미 고정한다(안전장치 2). 워커 제약 ① 이 따르게 한다 | "예외 매핑 정책은 구조에 영향을 주니 REQUIRED" 로 무조건 올리면 기존 패턴을 매번 베껴 적게 된다. precedent 가 없거나 직접 범위에서 경쟁할 때만 적는다 |
| 10 | 필터·변환을 stream 체인으로 쓸지 for 루프로 쓸지. 접근법은 "한 번 순회, 중간 컬렉션 없음" 으로 정해져 있다 | DELEGATED | 순회 횟수와 중간 구조물이 같다면 표현 차이 | 단, stream 이 중간 컬렉션을 만들거나 순회를 두 번 하게 되면 그것은 접근법 위반이며 REQUIRED 결정("중간 컬렉션 없음")이 이미 잡는다 |

## 판정 후 자기 점검

approach.md 를 쓴 뒤 REQUIRED 항목마다 다음을 확인한다.

- 갈리는 접근법 둘과 영향 하나가 **한 줄로** 적혀 있는가. 못 적으면 DELEGATED 로 내린다.
- 근거가 줄 범위 인용(`path:L40-L68`) 또는 표준 기법 + 이유인가.
- 그 아래 표현 수준(변수명, 지역 변수, 동등한 제어문, 작은 헬퍼 내부, overload, import·포맷)까지 적지 않았는가.
- 직접 범위에 명백한 단일 precedent 가 있는 문제를 다시 적고 있지 않은가.
- 고른 접근법이 후보 중 가장 단순한 통상 해법인가. 별도 상태 구조·새 추상화를 골랐다면 더 단순한 방법으로 요구를 표현할 수 없는 이유가 적혀 있는가.
- 금지 문구가 있다면 이유와 불변식(또는 허용 대안)이 붙어 있는가. 수단 이름만 금지한 줄은 지운다.

검증자는 반대 방향을 본다: 접근법이 갈리는 결정이 DELEGATED 로 남아 있으면 갈리는 접근법 둘과 영향 하나를 basis_refs 와 함께 적어 REVISE_DOC 으로 올리되, 어느 접근법을 고를지는 처방하지 않는다. 워커는 그런 결정을 문서와 직접 범위의 precedent 어디서도 찾지 못하면 스스로 고르지 않고 `DOC_GAP` 으로 돌려보낸다. 검증자와 워커가 둘 다 놓쳐 워커가 접근법을 골라 버린 경우 리뷰어가 `UNDECIDED_APPROACH` + `DOC_GAP` 으로 문서 단계로 되돌린다 — 리뷰어도 어느 접근법이 나은지는 적지 않는다.

실제 검증자가 이 경계를 잡는지는 `validator-cases/case-09-approach-undecided-scan`(BLOCK)·`case-10-expression-only-delegated`(PASS) 가, 리뷰어가 마지막 방어선으로 잡는지는 `reviewer-cases/case-12-undecided-approach`(REQUEST_CHANGES / UNDECIDED_APPROACH / DOC_GAP) 가 고정한다.
